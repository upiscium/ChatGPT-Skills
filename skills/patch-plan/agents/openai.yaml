interface:
  display_name: "Patch Plan"
  short_description: "Plan parallel issue workstreams and pull requests"
  default_prompt: "Use $patch-plan to turn these GitHub issues into dependency-aware parallel Codex workstreams with separate PRs."
