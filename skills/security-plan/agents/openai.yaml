interface:
  display_name: Security Plan
  short_description: Turn security findings into a Codex-ready patch plan
  default_prompt: Use @security-plan to turn this security audit into
    a safe, testable patch implementation plan.
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
