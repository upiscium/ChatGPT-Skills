interface:
  display_name: PR Merge Commands
  short_description: Generate guarded gh commands for reviewed PRs
  default_prompt: Use $pr-merge to generate safe gh commands that mark this reviewed
    PR ready and merge it.
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
