interface:
  display_name: "Development Loop"
  short_description: "Run parallel issue work through command checkpoints"
  default_prompt: "Use $dev-loop to advance these parallel workstreams and hand off guarded PR merge commands at the merge checkpoint."
